const config = {
  singleQuote: true, // 文字列はシングルクオートを使用
  semi: false, // 行末にセミコロンを付けない
  tabWidth: 2, // インデントはスペース2つ
  bracketSpacing: true, // オブジェクトの波括弧の間にスペースを入れる
  plugins: ['prettier-plugin-tailwindcss'], // Tailwindクラス順を自動整理
}

export default config
